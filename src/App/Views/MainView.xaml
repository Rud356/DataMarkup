<UserControl x:Class="App.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:App.Views" 
             xmlns:viewmodels="clr-namespace:App.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance viewmodels:MainViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
   
            

            <!-- Кнопки -->
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,10">
                <Button Command="{Binding OpenMarkupWindowCommand}" Content="Открыть проект" Margin="5" Width="100" Height="50"/>
                <Button Command="{Binding TestCommand}" Content="О программе" Margin="5" Width="100" Height="50"/>
            </StackPanel>
      
    </Grid>
</UserControl>
