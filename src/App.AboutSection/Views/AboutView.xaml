<UserControl x:Class="App.AboutSection.Views.AboutView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:viewmodels="clr-namespace:App.AboutSection.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance viewmodels:AboutViewModel}"
        d:Background="White"
        d:DesignHeight="720" d:DesignWidth="1280">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"></ColumnDefinition>
            <ColumnDefinition Width="8*"></ColumnDefinition>
            <ColumnDefinition Width="2*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        
        <Grid Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Height="Auto" Width="Auto" Margin="10,10,10,10">
            <TextBlock Grid.Column="1" HorizontalAlignment="Center">
                DataMarkup - ПО для разметки данных для нейросетей (v1.0.0)
            </TextBlock>
        </Grid>
        <!-- Добавляем кнопку "Назад" -->
        <Button Grid.Column="2" Command="{Binding GoBackCommand}" Content="Назад" VerticalAlignment="Bottom" Margin="10,10,10,10" HorizontalAlignment="Stretch"/>
    </Grid>
</UserControl>
